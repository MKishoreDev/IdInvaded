{
  "env": {
    "API_ID": {
      "description": "Your APP ID get this from my.telegram.org",
      "required": True
    },
    "API_HASH": {
      "description": "Your APP HASH get this from m>
      "required": True
    },
    "BOT_TOKEN": {
      "description": "Your BOT-TOKEN or STRING SESSION",
      "required": True
    },
    "IS_BOT": {
      "description": "True if BOT_TOKEN is bot token else False",
      "required": True
    },
    "MONGO_DB_URL": {
      "description": "YOUR MONGO DB URI",
      "required: True
    }
  }
}
